package com.example.mapart.plan.sweep;

/**
 * Skeleton for future leftover placement tracking during sweep execution.
 * Runtime integration is intentionally deferred.
 */
public final class LeftoverTracker {

    public LeftoverTracker() {
    }

    public int pendingCount() {
        return 0;
    }

    public void reset() {
        // Intentionally empty until sweep runtime wiring is introduced.
    }
}
